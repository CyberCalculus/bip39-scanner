[package]
name = "bip39-scanner"
version = "0.1.0"
edition = "2021"
description = "BIP39 mnemonic scanner - own crypto, no external crypto deps"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
